% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prLogisticGEE.R
\name{prLogisticGEE}
\alias{prLogisticGEE}
\title{Prevalence Ratios for Longitudinal Data -- GEE Models}
\usage{
prLogisticGEE(
  fit,
  standardisation = c("marginal", "conditional"),
  conf = 0.95,
  method = c("delta", "bootstrap"),
  data = NULL,
  R = 999L,
  ref_values = NULL,
  ref_continuous = c("median", "mean")
)
}
\arguments{
\item{fit}{A \code{geeglm} object fitted with \code{family = binomial} and
\code{link = "logit"} (or \code{family = binomial(link = "logit")}).}

\item{standardisation}{Character: \code{"marginal"} (default, recommended for
GEE) or \code{"conditional"}.}

\item{conf}{Confidence level. Default \code{0.95}.}

\item{method}{Inference method: \code{"delta"} (default) or \code{"bootstrap"}.}

\item{data}{Data frame (required when \code{method = "bootstrap"}).}

\item{R}{Number of bootstrap replicates (only used when
\code{method = "bootstrap"}). Default \code{999L}.}

\item{ref_values}{Named list of reference values. See \code{\link[=prLogisticDelta]{prLogisticDelta()}}.}

\item{ref_continuous}{\code{"median"} (default) or \code{"mean"}.}
}
\value{
A \code{"prLogistic"} object. See \code{\link[=prLogisticDelta]{prLogisticDelta()}}.
}
\description{
A convenience wrapper around \code{\link[=prLogisticDelta]{prLogisticDelta()}} for models fitted with
\code{\link[geepack:geeglm]{geepack::geeglm()}}. GEE provides population-averaged (marginal) estimates
suitable for longitudinal or clustered binary outcomes.
}
\details{
GEE accounts for within-subject correlation through a working correlation
structure (\code{corstr} argument of \code{geeglm()}). Common choices:
\describe{
\item{\code{"independence"}}{No correlation assumed (equivalent to GLM).}
\item{\code{"exchangeable"}}{Constant correlation across time points.}
\item{\code{"ar1"}}{First-order autoregressive; suitable for ordered time.}
\item{\code{"unstructured"}}{Estimates all pairwise correlations freely.}
}

The robust (sandwich) variance-covariance matrix returned by \code{vcov()} on
a \code{geeglm} object is used automatically, giving valid inference even when
the working correlation structure is misspecified.
}
\examples{
\dontshow{if (requireNamespace("geepack", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(geepack)
data(ohio, package = "geepack")

# Model respiratory symptoms over time with exchangeable correlation
fit_gee <- geeglm(
  resp  ~ smoke + age,
  family = binomial,
  id     = id,
  corstr = "exchangeable",
  data   = ohio
)

# Marginal PR (recommended for GEE)
prLogisticGEE(fit_gee)

# With bootstrap CIs
prLogisticGEE(fit_gee, method = "bootstrap", data = ohio, R = 99)
\dontshow{\}) # examplesIf}
}
\references{
Zeger, S. L. & Liang, K.-Y. (1986). Longitudinal data analysis for
discrete and continuous outcomes. \emph{Biometrics}, \strong{42}, 121-130.

H?jsgaard, S., Halekoh, U. & Yan, J. (2006). The R package geepack for
generalised estimating equations. \emph{Journal of Statistical Software},
\strong{15}(2), 1-11.

Amorim, L. D. & Ospina, R. (2021). \emph{An Acad Bras Cienc}, \strong{93}(4).
\doi{10.1590/0001-3765202120190316}
}
\seealso{
\code{\link[=prLogisticDelta]{prLogisticDelta()}}, \code{\link[geepack:geeglm]{geepack::geeglm()}}
}
