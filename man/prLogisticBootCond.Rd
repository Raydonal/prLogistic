% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prLogisticBoot.R
\name{prLogisticBootCond}
\alias{prLogisticBootCond}
\title{Bootstrap CI for Prevalence Ratios -- Conditional Standardisation}
\usage{
prLogisticBootCond(
  fit,
  data,
  conf = 0.95,
  R = 999L,
  ref_values = NULL,
  ref_continuous = c("median", "mean")
)
}
\arguments{
\item{fit}{A fitted model object of class \code{glm} (binomial family),
\code{glmerMod} (from \code{\link[lme4:glmer]{lme4::glmer()}}), \code{geeglm} (from \code{\link[geepack:geeglm]{geepack::geeglm()}}),
or \code{svyglm} (from \code{\link[survey:svyglm]{survey::svyglm()}}). Must use the logit link.}

\item{data}{Data frame used to fit \code{fit}. Required for bootstrapping.}

\item{conf}{Numeric scalar in (0, 1): confidence level. Default \code{0.95}.}

\item{R}{Integer: number of bootstrap replicates. Default \code{999}.}

\item{ref_values}{Named list of reference values for specific predictors,
e.g. \code{list(age = 40, bmi = 25)}. Overrides automatic reference-value
selection. For factor/dummy predictors the value should be \code{0} (the
default) or \code{1}.}

\item{ref_continuous}{Character string: how to compute the reference value
for continuous predictors when not supplied in \code{ref_values}. Either
\code{"median"} (default) or \code{"mean"}.}
}
\value{
An object of class \code{"prLogistic"} with components:
\describe{
\item{\code{table}}{Numeric matrix with columns \code{Estimate}, lower and upper CI.}
\item{\code{conf}}{Confidence level used.}
\item{\code{method}}{\code{"delta"}.}
\item{\code{standardisation}}{\code{"conditional"} or \code{"marginal"}.}
\item{\code{model_type}}{Class of the fitted model.}
\item{\code{call}}{The matched call.}
}
}
\description{
Estimates adjusted prevalence ratios (PR) using conditional standardisation
and obtains confidence intervals via bootstrap resampling (normal-
approximation and percentile methods).
}
\details{
At each bootstrap replicate the model is refitted on a resampled dataset
and conditional PRs are computed. Two CI types are returned:
\describe{
\item{Normal}{Bootstrap normal-approximation interval.}
\item{Percentile}{Empirical quantiles of the bootstrap distribution.}
}
Use \code{\link[=confint.prLogistic]{confint.prLogistic()}} with \code{type = "normal"} or \code{type = "percentile"}
to extract a single CI type.
}
\examples{
fit_glm <- glm(case ~ induced + spontaneous + parity,
               family = binomial, data = infert)

set.seed(42)
res <- prLogisticBootCond(fit_glm, data = infert, R = 199)
print(res)
plot(res)

}
\references{
Amorim, L. D. & Ospina, R. (2021). \emph{An Acad Bras Cienc}, \strong{93}(4).
\doi{10.1590/0001-3765202120190316}

Davison, A. C. & Hinkley, D. V. (1997). \emph{Bootstrap Methods and their
Application}. Cambridge University Press.
}
\seealso{
\code{\link[=prLogisticDelta]{prLogisticDelta()}}, \code{\link[=prLogisticBootMarg]{prLogisticBootMarg()}}
}
