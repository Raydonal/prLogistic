% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prLogisticDelta.R
\name{prLogisticDelta}
\alias{prLogisticDelta}
\title{Estimate Prevalence Ratios via Logistic Regression -- Delta Method}
\usage{
prLogisticDelta(
  fit,
  standardisation = c("conditional", "marginal"),
  conf = 0.95,
  ref_values = NULL,
  ref_continuous = c("median", "mean")
)
}
\arguments{
\item{fit}{A fitted model object of class \code{glm} (binomial family),
\code{glmerMod} (from \code{\link[lme4:glmer]{lme4::glmer()}}), \code{geeglm} (from \code{\link[geepack:geeglm]{geepack::geeglm()}}),
or \code{svyglm} (from \code{\link[survey:svyglm]{survey::svyglm()}}). Must use the logit link.}

\item{standardisation}{Character string: \code{"conditional"} (default) or
\code{"marginal"}. See \emph{Details}.}

\item{conf}{Numeric scalar in (0, 1): confidence level. Default \code{0.95}.}

\item{ref_values}{Named list of reference values for specific predictors,
e.g. \code{list(age = 40, bmi = 25)}. Overrides automatic reference-value
selection. For factor/dummy predictors the value should be \code{0} (the
default) or \code{1}.}

\item{ref_continuous}{Character string: how to compute the reference value
for continuous predictors when not supplied in \code{ref_values}. Either
\code{"median"} (default) or \code{"mean"}.}
}
\value{
An object of class \code{"prLogistic"} with components:
\describe{
\item{\code{table}}{Numeric matrix with columns \code{Estimate}, lower and upper CI.}
\item{\code{conf}}{Confidence level used.}
\item{\code{method}}{\code{"delta"}.}
\item{\code{standardisation}}{\code{"conditional"} or \code{"marginal"}.}
\item{\code{model_type}}{Class of the fitted model.}
\item{\code{call}}{The matched call.}
}
}
\description{
Estimates adjusted prevalence ratios (PR) and confidence intervals using
the delta method, from a fitted logistic regression model. Supports four
model types covering independent, clustered, longitudinal and complex-survey
data.
}
\details{
\subsection{Standardisation procedures}{

\strong{Conditional standardisation} fixes all covariates at their reference
values (median/mean for continuous, 0 for binary/dummy) and computes the
PR for each predictor by contrasting \emph{exposed} (predictor = 1) vs
\emph{unexposed} (predictor = 0) profiles:
\deqn{
  \widehat{PR}_j =
  \frac{\mathrm{expit}(\hat\beta_0 + \hat\beta_j + \sum_{k \neq j} \hat\beta_k r_k)}
       {\mathrm{expit}(\hat\beta_0 + \sum_{k \neq j} \hat\beta_k r_k)}
}
where \eqn{r_k} are the reference values of the remaining covariates.

\strong{Marginal standardisation} computes counterfactual prevalences using
the observed covariate distribution of the entire sample:
\deqn{
  \widehat{PR}_j =
  \frac{n^{-1}\sum_i \mathrm{expit}(\hat\eta_i^{(1)})}
       {n^{-1}\sum_i \mathrm{expit}(\hat\eta_i^{(0)})}
}
where \eqn{\hat\eta_i^{(1)}} and \eqn{\hat\eta_i^{(0)}} are the linear
predictors with predictor \eqn{j} set to 1 and 0, respectively.

Variance estimates use the delta method (first-order Taylor expansion) as
described in Oliveira et al. (1997) and Amorim & Ospina (2021).
}

\subsection{Baseline / reference category}{

By default, the reference level of each factor predictor is determined by
the contrasts of the fitted model (typically the first level of the
\code{factor()}). You can override this using \code{ref_values} for any predictor
column present in the model matrix.
}

\subsection{Supported model types}{\tabular{lll}{
   Class \tab Package \tab Use case \cr
   \code{glm} \tab stats \tab Independent observations \cr
   \code{glmerMod} \tab lme4 \tab Clustered / multilevel data \cr
   \code{geeglm} \tab geepack \tab Longitudinal / GEE \cr
   \code{svyglm} \tab survey \tab Complex survey designs \cr
}

}
}
\examples{
# --- Independent observations (glm) --- infert is a built-in dataset ----
# outcome: case (spontaneous abortion), prevalence ~33\%
fit_glm <- glm(case ~ induced + spontaneous + parity,
               family = binomial, data = infert)

# Conditional PR (continuous covariates at median)
prLogisticDelta(fit_glm, standardisation = "conditional")

# Marginal PR
prLogisticDelta(fit_glm, standardisation = "marginal")

# Custom reference values
prLogisticDelta(fit_glm,
                standardisation = "conditional",
                ref_values = list(parity = 2))

\donttest{
# --- Clustered data (glmer) ---------------------------------------------
library(lme4)
fit_glmer <- glmer(case ~ induced + spontaneous + (1 | stratum),
                   family = binomial, data = infert)
prLogisticDelta(fit_glmer, standardisation = "marginal")

# --- Longitudinal / GEE -------------------------------------------------
library(geepack)
data(ohio, package = "geepack")
fit_gee <- geeglm(resp ~ smoke + age,
                  family  = binomial,
                  id      = id,
                  corstr  = "exchangeable",
                  data    = ohio)
prLogisticDelta(fit_gee, standardisation = "marginal")

# --- Complex survey design ----------------------------------------------
library(survey)
data(api, package = "survey")
dclus2 <- svydesign(id = ~dnum + snum, fpc = ~fpc1 + fpc2, data = apiclus2)
fit_svy <- svyglm(sch.wide ~ meals + stype,
                  design = dclus2, family = quasibinomial)
prLogisticDelta(fit_svy, standardisation = "conditional")
}

}
\references{
Amorim, L. D. & Ospina, R. (2021). Prevalence ratio estimation using R.
\emph{Anais da Academia Brasileira de Ciencias}, \strong{93}(4), e20190316.
\doi{10.1590/0001-3765202120190316}

Oliveira, N. F., Santana, V. S. & Lopes, A. A. (1997). Razoes de
proporcoes e uso da regressao log?stica em estudos transversais.
\emph{Revista de Sa?de P?blica}, \strong{31}, 90-99.

Wilcosky, T. C. & Chambless, L. E. (1985). A comparison of direct
adjustment and regression adjustment of epidemiologic measures.
\emph{Journal of Chronic Diseases}, \strong{38}, 849-856.
}
\seealso{
\code{\link[=prLogisticBootCond]{prLogisticBootCond()}}, \code{\link[=prLogisticBootMarg]{prLogisticBootMarg()}},
\code{\link[=prLogisticGEE]{prLogisticGEE()}}, \code{\link[=prLogisticSurvey]{prLogisticSurvey()}}
}
