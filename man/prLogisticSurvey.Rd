% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prLogisticSurvey.R
\name{prLogisticSurvey}
\alias{prLogisticSurvey}
\title{Prevalence Ratios for Complex Survey Data}
\usage{
prLogisticSurvey(
  fit,
  standardisation = c("conditional", "marginal"),
  conf = 0.95,
  ref_values = NULL,
  ref_continuous = c("median", "mean")
)
}
\arguments{
\item{fit}{A \code{svyglm} object fitted with \code{family = quasibinomial} (or
\code{binomial}) and the logit link.}

\item{standardisation}{Character: \code{"conditional"} (default) or
\code{"marginal"}.}

\item{conf}{Confidence level. Default \code{0.95}.}

\item{ref_values}{Named list of reference values. See \code{\link[=prLogisticDelta]{prLogisticDelta()}}.}

\item{ref_continuous}{\code{"median"} (default) or \code{"mean"}.}
}
\value{
A \code{"prLogistic"} object. See \code{\link[=prLogisticDelta]{prLogisticDelta()}}.
}
\description{
A convenience wrapper around \code{\link[=prLogisticDelta]{prLogisticDelta()}} for logistic regression
models fitted on complex survey data using \code{\link[survey:svyglm]{survey::svyglm()}}.
}
\details{
\code{svyglm()} incorporates sampling weights and complex design features
(stratification, clustering, finite-population corrections) into parameter
estimation. The design-consistent variance-covariance matrix is extracted
automatically via \code{vcov()} and used in the delta-method calculations.

\strong{Note:} bootstrap resampling for survey data requires design-aware
resampling (e.g., survey bootstrap, balanced repeated replication).
This is currently not automated; use \code{\link[=prLogisticDelta]{prLogisticDelta()}} with a
bootstrap-replicate survey design if needed.
}
\examples{
\dontshow{if (requireNamespace("survey", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(survey)
data(api, package = "survey")

# Create binary outcome
apiclus2$target_met <- as.numeric(apiclus2$sch.wide == "Yes")

# Stratified two-stage cluster sample
dclus2 <- svydesign(
  id   = ~dnum + snum,
  fpc  = ~fpc1 + fpc2,
  data = apiclus2
)

fit_svy <- svyglm(
  target_met ~ meals + stype,
  design = dclus2,
  family = quasibinomial
)

prLogisticSurvey(fit_svy, standardisation = "conditional")
prLogisticSurvey(fit_svy, standardisation = "marginal")
\dontshow{\}) # examplesIf}
}
\references{
Lumley, T. (2004). Analysis of complex survey samples.
\emph{Journal of Statistical Software}, \strong{9}(1), 1-19.

Lumley, T. (2010). \emph{Complex Surveys: A Guide to Analysis Using R}.
Wiley, New Jersey.

Amorim, L. D. & Ospina, R. (2021). \emph{An Acad Bras Cienc}, \strong{93}(4).
\doi{10.1590/0001-3765202120190316}
}
\seealso{
\code{\link[=prLogisticDelta]{prLogisticDelta()}}, \code{\link[survey:svyglm]{survey::svyglm()}}
}
